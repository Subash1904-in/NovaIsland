<UserControl
    x:Class="NovaIsland.Panels.AiPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NovaIsland.Panels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="16" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="OutputTextBlock" TextWrapping="Wrap" FontSize="16" />
        </ScrollViewer>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputTextBox" Grid.Column="0" PlaceholderText="Ask the assistant..." Margin="0,0,8,0" KeyDown="InputTextBox_KeyDown" />
            <Button x:Name="SendButton" Grid.Column="1" Content="Send" Click="SendButton_Click" />
        </Grid>
    </Grid>
</UserControl>
